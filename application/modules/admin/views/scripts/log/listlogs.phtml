<? if (isset($this->error)) { ?>
    <div class="error"><?= $this->error ?></div>
<? } ?>
<div id="divmodificaciones" class="clistado">
<table id="tablemodifications">
    <thead>
        <tr>
            <th> <?= $this->language->_("ID") ?> </th>
            <th> <?= $this->language->_("Nivel") ?> </th>
            <th> <?= $this->language->_("Mensaje") ?> </th>
        </tr>
    </thead>
    <tbody>
    <? if (count($this->paginator)) { ?>
        <? foreach($this->paginator as $log) { ?>
        <tr> <td> <?= $log->id() ?> </td>
            <td> <?= $log->nivel() ?> </td>
            <td> <?= $log->msg() ?> </td>
        </tr>
        <? } ?>
    <? } ?>
    </tbody>
</table>
</div>

<?= $this->paginationControl($this->paginator, 'Sliding', 'paginationControl.phtml'); ?>

<script type="text/javascript">
YAHOO.util.Event.addListener(window, "load", function() {
    YAHOO.example.EnhanceFromMarkup = new function() {
        var myColumnDefs = [
            {key:"id",label:" <?= $this->language->_("ID") ?> ",sortable:true},
            {key:"level",label:" <?= $this->language->_("Nivel") ?> ", sortable:true},
            {key:"msg",label:" <?= $this->language->_("Mensaje") ?> ", sortable:false}
        ];

        this.myDataSource = new YAHOO.util.DataSource(YAHOO.util.Dom.get("tablemodifications"));
        this.myDataSource.responseType = YAHOO.util.DataSource.TYPE_HTMLTABLE;
        this.myDataSource.responseSchema =
        {
            fields:
            [
                {key:"id"},
                {key:"level"},
                {key:"msg"}
            ]
        };

        this.myDataTable = new YAHOO.widget.DataTable("divmodificaciones", myColumnDefs, this.myDataSource,
                {sortedBy:{key:"id",dir:"desc"}}
        );
    };
});
</script>

