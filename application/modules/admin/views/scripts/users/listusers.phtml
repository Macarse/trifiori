<div id="divlistusers" class="clistado">
	<table id="tableusers">
		<thead>
			<tr><th>Nombre</th> <th>Usuario</th> <th>Idioma</th> <th>Modificar</th> <th>Eliminar</th> </tr>
		</thead>
		<tbody>
    <? if (count($this->paginator)) { ?>
        <? foreach($this->paginator as $user) { ?>
			<tr><td> <?= $user["NOMBRE_USU"] ?> </td>
				<td> <?= $user["USUARIO_USU"] ?> </td>
				<td> <?= $user["IDIOMA_USU"] ?> </td>
				<td>
					<a href="<?= "/admin/users/modusers/id/" . $user["CODIGO_USU"]?>">Modificar</a>
				</td>
				<td>
					<a href="<?= "/admin/users/removeusers/id/" . $user["CODIGO_USU"]?>">Eliminar</a>
				</td>
			</tr>
			<? } ?>
        <? } ?>
		</tbody>
	</table>
</div>

<?= $this->paginationControl($this->paginator, 'Sliding', 'paginationControl.phtml'); ?>
<script type="text/javascript">
YAHOO.util.Event.addListener(window, "load", function() {
    YAHOO.example.EnhanceFromMarkup = new function() {
        var myColumnDefs = [
            {key:"name",label:"Nombre",sortable:true},
            {key:"user",label:"Usuario", sortable:true},
            {key:"language",label:"Idioma", sortable:false},
			{key:"modify",label:"Modificar", sortable:false},
			{key:"eliminate",label:"Eliminar", sortable:false}
        ];

        this.myDataSource = new YAHOO.util.DataSource(YAHOO.util.Dom.get("tableusers"));
        this.myDataSource.responseType = YAHOO.util.DataSource.TYPE_HTMLTABLE;
        this.myDataSource.responseSchema = {
            fields: [{key:"name"},
		             {key:"user"},
         		     {key:"language"},
		  	   		 {key:"modify"},
					 {key:"eliminate"}]
	        };

        this.myDataTable = new YAHOO.widget.DataTable("divlistusers", myColumnDefs, this.myDataSource,
                {sortedBy:{key:"name",dir:"desc"}}
        );
    };
});
</script>
